# 第三方服务使用现状与公司主体更换指引

> 项目：寰球联采 (CRMEB-JAVA-ET)
> 生成日期：2026-05-16

---

## 一、当前正在使用的服务

### 1.1 配置文件中有真实凭证的

| 服务 | 凭证/配置 | 配置位置 |
|------|-----------|----------|
| MySQL 数据库 | `java_et` / `111111`（服务器环境） | `application.yml` |
| Redis 缓存 | 1Panel-redis-QjWO + 密码 | `application.yml` |
| 阿里云邮件推送 | `server@hqlccn.com` + SMTP 密码 | `application.yml` |
| 高德地图 (Amap) | iOS Key + Android Key 已配置 | `manifest.json` |
| 腾讯地图 (QQMap) | Key 已配置 | `manifest.json` H5 SDK |
| 微信小程序 | appid: `wxcda5a2b1b98b6a95` | `manifest.json` mp-weixin |
| 微信 App 支付/登录/分享 | appid: `wxa83d6fab40cab13f` | `manifest.json` OAuth/Payment/Share |
| 字节跳动小程序 | appid: `tt1688603589b1e71d01` | `manifest.json` mp-toutiao |

### 1.2 后台数据库动态配置的（在 `eb_system_config` 表中）

以下服务代码已对接，但凭证和开关在管理后台配置，当前是否启用取决于数据库中的值：

| 服务 | 数据库配置键 | 说明 |
|------|-------------|------|
| 阿里云 OSS | `alUploadUrl`, `alAccessKey`, `alSecretKey` | 云存储（图片上传） |
| 腾讯云 COS | `txUploadUrl`, `txAccessKey`, `txSecretKey` | 云存储（图片上传） |
| 七牛云 Kodo | `qnUploadUrl`, `qnAccessKey`, `qnSecretKey` | 云存储（图片上传） |
| 阿里云短信 | `aliyun_sms_key_id`, `aliyun_sms_key_secret`, `aliyun_sms_sign_name` | 短信验证码/通知 |
| 阿里云物流 | `logistics_app_key`, `logistics_app_secret` | 快递物流轨迹查询 |
| 微信支付 | `wechat_pay_switch`（代码配置中 appId/mchId 为占位符） | 微信支付 |
| PayPal | `paypal_client_id`, `paypal_client_secret`, `paypal_switch` | 海外支付 |
| Stripe | `stripe_api_key`, `stripe_switch` | 海外支付（美元） |
| Google 登录 | `google_client_id`, `google_client_secret`, `google_open` | 第三方登录 |
| Facebook 登录 | `facebook_appid`, `facebook_open` | 第三方登录 |
| X (Twitter) 登录 | `x_oauth2_client_id`, `x_oauth2_client_secret` | 第三方登录 |
| 商品导入 (99Api) | `copy_product_apikey` | 从淘宝/京东/拼多多等平台复制商品 |

### 1.3 代码中有但当前未启用的

| 服务 | 原因 |
|------|------|
| 支付宝 | `pom.xml` 中依赖已注释 |
| MongoDB | `pom.xml` 中依赖已注释 |

---

## 二、公司主体变更 - 需更换的第三方服务清单

### 2.1 必须更换（绑定公司资质/营业执照）

| 服务 | 变更内容 | 风险等级 |
|------|----------|----------|
| **微信支付商户号** | 商户号绑定营业执照，需重新申请 | 🔴 高 |
| **微信小程序 AppID** | 小程序绑定公司主体，需重新注册 | 🔴 高 |
| **微信开放平台 AppID** | 移动端微信登录/分享，绑定公司主体 | 🔴 高 |
| **支付宝商家**（以后启用时） | 绑定营业执照 | 🟡 中 |
| **PayPal Business 账号** | 需要公司资质认证 | 🟡 中 |
| **Stripe 账号** | 需要公司资质认证 | 🟡 中 |

### 2.2 可能需要更换（看账号归属）

如果原账号是用旧公司身份注册的，则需要更换或过户：

| 服务 | 说明 |
|------|------|
| **阿里云账号**（OSS/短信/物流/邮件） | 账号归属旧公司 → 需迁移或重新申请 |
| **腾讯云账号**（COS） | 同上 |
| **七牛云账号** | 同上 |
| **高德地图 Key** | Key 本身不绑定主体，但原账号是旧公司的需重新申请 |
| **腾讯地图 Key** | 同上 |

### 2.3 不需要更换的

| 类型 | 说明 |
|------|------|
| MySQL / Redis | 自建基础设施，与公司主体无关 |
| 云服务器 / IP | 物理设施，不变 |
| 所有开源框架依赖 | Spring Boot、MyBatis-Plus、Vue、Nuxt 等开源软件 |

---

## 三、域名变更涉及的配置清单

若公司主体变更同时需要更换域名（当前域名 `hqlccn.com`），以下文件均需修改：

### 3.1 Java 后端配置

| 文件路径 | 需修改内容 |
|----------|-----------|
| `et_java/crmeb-admin/src/main/resources/application.yml` | 微信支付回调 `notifyUrl`、`refundNotifyUrl` |
| `et_java/crmeb-admin/src/main/resources/application-prod.yml` | `paypalReturnUri`、`paypalPcReturnUri` |
| `et_java/crmeb-front/src/main/resources/application.yml` | `paypalReturnUri`、`paypalPcReturnUri` |
| `et_java/crmeb-front/src/main/resources/application-prod.yml` | `paypalReturnUri`、`paypalPcReturnUri` |

### 3.2 Uniapp 移动端配置

| 文件路径 | 需修改内容 |
|----------|-----------|
| `et_uniapp/config/app.js` | API 请求域名（非 H5 环境） |
| `et_uniapp/manifest.json` | 微信开放平台 UniversalLinks |
| `et_uniapp/manifest.json` H5 proxy | 代理目标地址 |
| `et_uniapp/manifest.json` mp-weixin | 小程序 appid（如需重新注册） |

### 3.3 前端配置

| 文件路径 | 需修改内容 |
|----------|-----------|
| `et_mer_admin/.env.development` | API 地址 |
| `et_plat_admin/.env.development` | API 地址 |
| `et_pc/nuxt.config.js` | API 代理地址 |

### 3.4 服务器 / 运维

| 项目 | 说明 |
|------|------|
| **Nginx** | 新域名指向、SSL 证书更换 |
| **阿里云邮件** | 发信域名变更、发信地址变更（`server@hqlccn.com`） |
| **高德/腾讯地图** | 回调域名白名单更新 |
| **微信公众平台** | 小程序服务器域名、业务域名更新 |
| **微信开放平台** | 移动应用 UniversalLinks 更新 |
| **PayPal/Stripe** | Webhook 回调地址更新 |

---

## 四、变更流程建议

```
1. 新公司资质准备
   ├── 营业执照
   ├── 对公银行账户
   └── 域名备案（如需换域名）

2. 账号重新申请
   ├── 微信商户号 + 小程序 + 开放平台
   ├── PayPal / Stripe 商户账号
   └── 云服务商账号（阿里/腾讯/七牛）

3. 开发配置更新
   ├── 修改所有配置文件中的凭证和域名
   ├── 更新 Nginx / SSL 证书
   └── 重新打包部署

4. 切换上线
   ├── DNS 解析切换
   ├── 微信回调域名验证
   └── 全流程回归测试
```
